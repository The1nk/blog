apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blog
  template:
    metadata:
      labels:
        app: blog
    spec:
      containers:
      - name: blog
        image: canister.io:5000/the1nk/blog
        ports:
          - containerPort: 80
      imagePullSecrets:
      - name: regcred